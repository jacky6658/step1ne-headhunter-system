#!/bin/bash
# PostgreSQL 連接腳本
# 使用方式：bash scripts/connect-postgres.sh
# 
# 注意：請複製此文件為 connect-postgres.sh 並填入實際的資料庫資訊

# 資料庫連接資訊（從環境變數或直接設置）
DB_HOST="${DB_HOST:-localhost}"
DB_PORT="${DB_PORT:-5432}"
DB_NAME="${DB_NAME:-step1ne}"
DB_USER="${DB_USER:-step1ne}"
DB_PASSWORD="${DB_PASSWORD:-}"

# 連接字串
CONNECTION_STRING="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}"

echo "正在連接到 PostgreSQL 資料庫..."
echo "主機: ${DB_HOST}"
echo "資料庫: ${DB_NAME}"
echo ""
echo "連接成功後，您可以執行 SQL 命令。"
echo "輸入 \\q 退出 psql"
echo ""

# 使用 psql 連接
PGPASSWORD="${DB_PASSWORD}" psql "${CONNECTION_STRING}"
