{
  "install_command": "npm install && (apt-get update -qq && apt-get install -y python3-pip && pip3 install requests beautifulsoup4 lxml --break-system-packages -q 2>/dev/null || pip3 install requests beautifulsoup4 lxml -q 2>/dev/null || true)",
  "start_command": "node server.js"
}
