{
  "install_command": "npm install && apt-get update -qq && apt-get install -y python3-pip && pip3 install playwright && python3 -m playwright install --with-deps chromium",
  "start_command": "node server.js"
}
